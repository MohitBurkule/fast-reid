_BASE_: ../Base-bagtricks.yml

MODEL:
  BACKBONE:
    NAME: build_resnest_backbone

DATASETS:
  NAMES: ("DukeMTMC",)
  TESTS: ("DukeMTMC",)

OUTPUT_DIR: logs/dukemtmc/bagtricks_S50

SOLVER:
  AMP:
    ENABLED: False  # Disable AMP due to PyTorch 2.5 compatibility issues
